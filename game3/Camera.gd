extends Camera


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
	set_process(true)

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
	#self.look_at(self.get_parent().get_parent().get_parent().get_children()[1].get_children()[0].get_children()[0].get_children()[0]., Vector3(0.0, 1.0, 0.0))
